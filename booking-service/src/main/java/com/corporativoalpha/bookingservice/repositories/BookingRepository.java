package com.corporativoalpha.bookingservice.repositories;import com.corporativoalpha.bookingservice.models.Booking;import org.springframework.data.jpa.repository.JpaRepository;import java.time.LocalDate;import java.util.List;public interface BookingRepository extends JpaRepository<Booking, Long> {List<Booking> findBySpaceIdAndBookingDate(Long spaceId, LocalDate bookingDate);List<Booking> findByUserId(Long userId);}
