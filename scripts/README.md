# Scripts

Utilidades del proyecto (Fase 3: andamiaje).

- `setup.sh` — crea `.env` desde `.env.example` e instala dependencias.

Scripts previstos para fases posteriores: `migrate.sh`, `seed.sh`, `test.sh`, `db-reset.sh`.
