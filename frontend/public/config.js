// Runtime configuration for NeoWallet Frontend
// This file is loaded at runtime, not build time
window.APP_CONFIG = {
  ACCOUNTS_API_URL: '/api/accounts',
  PROCESSOR_API_URL: '/api/processor',
};

// Made with Bob
