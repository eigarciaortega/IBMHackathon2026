# Docker

Configuración de contenedores (Fase 3: andamiaje).

- `Dockerfile.backend` / `Dockerfile.frontend` — placeholders; los builds reales se definen en la **Fase 10**.
- `postgres/` — espacio para scripts de inicialización de PostgreSQL si se requieren.
- La orquestación está en `../docker-compose.yml` (define postgres + backend + frontend; aún no funcional end-to-end).
