/**
 * @officespace/shared-types — punto de entrada (Fase 3: andamiaje).
 *
 * Aquí se exportarán en fases posteriores los enums y contratos compartidos, p. ej.:
 *   - RoleType (ADMIN, COLLABORATOR)
 *   - UserStatus (ACTIVE, INACTIVE, BLOCKED)
 *   - SpaceStatus (AVAILABLE, MAINTENANCE, INACTIVE)
 *   - BookingStatus (CONFIRMED, CANCELLED, FINISHED, NO_SHOW, PENDING_APPROVAL[reservado])
 *   - DTOs/contratos de respuesta (SuccessResponse, ErrorResponse, PaginatedResponse)
 *
 * Sin definiciones todavía.
 */
export {};
